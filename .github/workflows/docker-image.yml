name: Deploy Docker

on: [workflow_dispatch]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Login to Docker
      run: docker login -u bufferOverflowRegistry -p +GOAy5TG0OpJG3/uVC3RR3O7TdDTDi=p
    - name: Build Docker Image
      run: docker build -t bufferoverflowregistry.azurecr.io/services:latest
